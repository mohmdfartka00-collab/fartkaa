# ربط فرتكه بـ Supabase + الصور + Paymob

## ما تم تجهيزه
- المشروع يستخدم مشروع Supabase الحالي: `kycvxqjjdkoatemjxaoz`.
- مفتاح Supabase الـ Publishable موجود في `supabase-config.js`، وهو النوع المناسب للمتصفح مع RLS.
- صور المنتجات وصورة الواجهة تترفع إلى Supabase Storage داخل bucket اسمه `product-images`.
- الرفع مسموح للمدير فقط، والقراءة للصور عامة.
- إضافة/تعديل المنتجات تستخدم رابط الصورة المرفوع تلقائيًا.
- الدفع بالبطاقة مربوط مع Supabase Edge Function باسم `create-paymob-intention`.
- استقبال نتيجة الدفع مربوط مع `paymob-webhook` ويستخدم HMAC-SHA512 قبل اعتبار الطلب مدفوعًا.
- السعر النهائي للطلب يُحسب داخل قاعدة البيانات، وليس من بيانات المتصفح.

## 1) تشغيل قاعدة البيانات
في Supabase افتح **SQL Editor** وشغّل الملف:

`supabase_migration_v2.sql`

شغّله كاملًا مرة واحدة.

## 2) جعل حسابك مديرًا
بعد إنشاء حسابك في الموقع، نفّذ في SQL Editor:

```sql
update public.profiles
set role='admin'
where id=(select id from auth.users where email='بريدك@example.com');
```

لا تضع بريدك الحقيقي داخل ملفات الموقع العامة.

## 3) نشر وظائف الدفع
ثبّت Supabase CLI وسجّل الدخول ثم من مجلد المشروع:

```bash
supabase login
supabase link --project-ref kycvxqjjdkoatemjxaoz
supabase functions deploy create-paymob-intention
supabase functions deploy paymob-webhook --no-verify-jwt
```

## 4) مفاتيح Paymob — لا تضعها في الموقع
من لوحة Paymob خذ مفاتيح **Test** أولًا. Paymob يوصي باستخدام Intention API، وإنشاء عملية الدفع من الخلفية، ثم فتح Unified Checkout بالمفتاح العام وclient secret. نتيجة الدفع النهائية يجب اعتمادها من الـWebhook وليس من رابط الرجوع في المتصفح.

في Supabase أضف Secrets التالية:

```text
PAYMOB_BASE_URL=https://accept.paymob.com
PAYMOB_SECRET_KEY=sk_test_...
PAYMOB_PUBLIC_KEY=pk_test_...
PAYMOB_INTEGRATION_ID_CARD=رقم_Integration_ID_للبطاقة
PAYMOB_HMAC_SECRET=مفتاح_HMAC
APP_URL=https://دومين-الموقع-الفعلي
```

**ممنوع** وضع `PAYMOB_SECRET_KEY` أو `PAYMOB_HMAC_SECRET` داخل `index.html` أو `script.js` أو `payment-config.js`.

## 5) روابط Paymob
بعد نشر الـFunctions، استخدم:

```text
https://kycvxqjjdkoatemjxaoz.supabase.co/functions/v1/create-paymob-intention
```

وفي Paymob اجعل Webhook للـIntegration على:

```text
https://kycvxqjjdkoatemjxaoz.supabase.co/functions/v1/paymob-webhook
```

ويُفضّل أيضًا وضع نفس Webhook في `notification_url` الذي ترسله الوظيفة.

## 6) اختبار الدفع
ابدأ بمفاتيح Test وIntegration ID الخاصة بالـTest. اعمل طلبًا من الموقع واختر البطاقة. الموقع سينشئ الطلب أولًا، ثم يفتح صفحة Paymob. بعد نجاح العملية، Paymob يرسل Webhook، والوظيفة تتحقق من HMAC وتحوّل الطلب إلى `paid`.

بعد نجاح الاختبار فقط انتقل إلى مفاتيح Live وIntegration IDs الخاصة بالـLive.

## 7) الصور
من لوحة المدير:
- إضافة منتج → اختر صورة من الجهاز → حفظ.
- تعديل منتج → اختر صورة جديدة → حفظ.
- واجهة الموقع → ارفع صورة رئيسية.

الصور تذهب إلى Storage، ولا تحتاج لرفعها يدويًا إلى موقع الاستضافة.
