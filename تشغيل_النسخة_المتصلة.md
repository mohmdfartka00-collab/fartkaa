# تشغيل فرتكه بعد ربط Supabase

1. في Supabase افتح SQL Editor ثم New query.
2. افتح الملف `supabase_migration.sql` وانسخ محتواه كاملًا إلى المحرر.
3. اضغط Run وانتظر Success.
4. أنشئ حسابك من الموقع بالبريد الإلكتروني.
5. من SQL Editor شغّل:
   update public.profiles set role='admin'
   where id=(select id from auth.users where email='بريدك');
   واستبدل بريدك بالبريد الذي سجلت به.
6. ارفع كل ملفات هذه النسخة إلى مستودع GitHub Pages مع استبدال الملفات القديمة.
7. لا ترفع أي كلمة مرور أو مفتاح سري. ملف supabase-config.js يحتوي على المفتاح العام فقط.

ملاحظة: إذا كان تأكيد البريد الإلكتروني مفعلًا، يجب تأكيد البريد قبل تسجيل الدخول.
